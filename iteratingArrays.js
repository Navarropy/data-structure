// Iterating through an array using a forEach loop.
myList.forEach((item) => {console.log(item)})

	// You can also use without parentheses if it's a shorter code, like our case.
	myList.forEach(item => console.log(item))

// Iterating through an array using a for loop.
for(item of myList) {
	console.log(item)
}